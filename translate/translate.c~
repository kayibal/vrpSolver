#include<fstream.h>
#include<string>

int main(int argc,char**argv)
{
    char* name;
    char* type;
    char* comment;
    int dimension
    if(argc<2)
    {
        printf("Usage: translate <.vrp>");
        return -1;
    }

}
